[package]
name = "tundra-ui"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
geojson = "1.0"
ratatui = "0.29"
zip = { version = "6", default-features = false, features = ["deflate"] }
