[package]
name = "storage"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
platform = { path = "../platform" }

serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.9"
