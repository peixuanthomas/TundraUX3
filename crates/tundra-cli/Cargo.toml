[package]
name = "tundra-cli"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[[bin]]
name = "tundra-cli"
path = "src/main.rs"

[dependencies]
tundra-platform = { path = "../tundra-platform" }
